import { VerticalBox } from "std-widgets.slint";
import { SaveMnemonic } from "./screens/save-mnemonic.slint";

export component AppWindow inherits Window {
    width: 400px;
    height: 600px;
    SaveMnemonic {
        
    }
  
}
